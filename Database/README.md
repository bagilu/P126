# SQL 執行順序

全新部署只需執行00_P126_Install.sql（含01–08，交易包覆），再執行99健康檢查。

01–08分檔供維護參考，勿只執行前半段就讓網站上線。00會一次設定表、函式、RLS與撤銷權限，失敗則整筆回滾。

90為本版權限修復，99唯讀。全部只限定P126物件；auth.users僅作FK引用。

本版尚無migration；同版00可重複執行，但後續結構更新需另寫migration。
